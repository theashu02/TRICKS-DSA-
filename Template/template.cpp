#include <bits/stdc++.h>
#pragma GCC optimize("O3", "unroll-loops")
#define ll long long
#define pb push_back
#define mp make_pair
#define all(v) v.begin(), v.end()
#define lln long long int
#define mpp map<int, int>
#define umpp unordered_map<int, int>
const int mod = 1e9 + 7;
// Define debugging macros (if necessary)
#ifndef ONLINE_JUDGE
#define debug(x) cerr << #x << " = " << x << "\n";
#else
#define debug(x)
#endif

using namespace std;

auto init = []()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    return 'c';
}();

class Solution
{
public:
    void solve()
    {
        // write your code
    }
};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        Solution obj;
        obj.solve();
    }

    return 0;
}